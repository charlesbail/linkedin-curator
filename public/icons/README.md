# Icons

Place extension icons here (`icon16.png`, `icon48.png`, `icon128.png`), then
add an `icons` and `action.default_icon` block back to `manifest.json`
pointing at these files (e.g. `public/icons/icon16.png`).

No placeholder binaries are checked in on purpose — add real artwork when
it's ready.
